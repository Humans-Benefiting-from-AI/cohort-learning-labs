# Google Analytics — consumed by components/GoogleAnalytics.tsx; omitted when unset.
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Calendly event for the 30-minute consultation. Embeds on /contact#book.
# In Calendly: create a 30-minute event, connect Google Calendar under
# Integrations so busy times never appear as open, then set weekly hours.
# Optional override. The site defaults to:
# https://calendly.com/andrea-schulman-coaching/consultation-with-elie
NEXT_PUBLIC_CALENDLY_URL=
